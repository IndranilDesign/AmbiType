# AmbiType

## Local typing corpus
Run this once after adding EPUB files to `public/Books/`:

```bash
npm run corpus:build
```

This generates cleaned paragraph corpus files in `public/corpus/books/` and writes `public/corpus/index.json`.
